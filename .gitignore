# The theme directory keeps its own history; the outer repository tracks its files too.
docs.qq.theme/.git/
docs.qq.theme/.git-nested-backup/
.browser-qa/
github.theme/node_modules/
# WorkBuddy workspace metadata (session notes, memory, project data).
.workbuddy/
